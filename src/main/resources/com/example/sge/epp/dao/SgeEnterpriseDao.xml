<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sge.epp.dao.SgeEnterpriseDao">
    
    <select id="get" parameterType="Integer" resultType="SgeEnterprise">
        SELECT * FROM sge_enterprise 
        WHERE id_enterprise = #{idEnterprise};
    </select>
    
</mapper>